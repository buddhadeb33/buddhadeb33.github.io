# Archive

Older versions of this portfolio, kept for reference. Not linked from the live site nav.

## `pre-redesign/`

Snapshot of the site **before** the AI Architect redesign (particles.js, Bootstrap dual nav, purple template look, standalone Skills/Experience/Education pages).

Taken from git commit at archive time (`git archive HEAD` of the previous live tree).

### Preview locally

```bash
cd archive/pre-redesign
python3 -m http.server 8001
```

Open http://localhost:8001
